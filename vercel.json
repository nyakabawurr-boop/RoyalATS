{
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install --no-audit --no-fund",
  "framework": "nextjs",
  "regions": ["iad1"],
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}
